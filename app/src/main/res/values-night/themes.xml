<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PhoneMouse" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryVariant">#CCCCCC</item>
        <item name="colorOnPrimary">@android:color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#999999</item>
        <item name="colorSecondaryVariant">#999999</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <item name="android:colorControlHighlight">#20FFFFFF</item>
        <item name="colorAccent">@android:color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/white</item>

        <!-- Custom Attributes -->
        <item name="trackpadBackgroundColor">#2C2C2E</item>
        <item name="controlBackgroundColor">#444444</item>
        <item name="controlIconColor">@android:color/white</item>
        <item name="controlTextColor">@android:color/white</item>
    </style>
</resources>